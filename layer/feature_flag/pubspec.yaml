name: feature_flag
description: "Flutter package for feature flags in project."
publish_to: 'none'

environment:
  sdk: ^3.8.1

resolution: workspace

dependencies:
  cache:
    path: ../cache
  core:
    path: ../core
  dependency_injection:
    path: ../dependency_injection
  equatable: ^2.0.7
  error_reporter:
    path: ../error_reporter
  firebase:
    path: ../firebase
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  log_reporter:
    path: ../log_reporter
  use_case:
    path: ../use_case

dev_dependencies:
  build_runner: ^2.5.4
  flutter_test:
    sdk: flutter
  json_serializable: ^6.9.5
  mocktail: ^1.0.4

flutter:
  uses-material-design: true