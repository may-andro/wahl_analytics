name: core
description: Provides core functionality for the app.
publish_to: 'none'

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  collection: ^1.19.0
  flutter:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter